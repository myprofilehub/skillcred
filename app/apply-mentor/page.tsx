import { MentorApplicationForm } from "@/components/mentor-application-form";

export const metadata = {
    title: "Apply to be a Mentor | SkillCred",
    description: "Join SkillCred as a freelance technical mentor and impart knowledge.",
};

export default function ApplyMentorPage() {
    return <MentorApplicationForm />;
}
